# Security

## Trust Boundary

ローカルブラウザと同梱の静的ファイルだけで動作します。認証、認可、秘密情報、個人情報、外部サービスはありません。

## Input Validation

経営入力は定義済みの刻みと上下限に制限します。静的サーバーは公開ルート外へのパストラバーサルを拒否します。

## Data and Logging

状態はメモリだけに保持し、永続化・外部送信を行いません。HTTPサーバーは起動URL以外のアプリケーションデータをログへ出しません。

## Dependencies

実行時・開発時ともに外部パッケージを使用しません。Node.jsランタイムの更新は利用環境側で管理します。
